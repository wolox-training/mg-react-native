import React from "react";
import { Text } from "react-native";
import { styles } from "../../styles/styles";

class TextInput extends React.Component {
  render() {
    return <Text style={styles.newItem} />;
  }
}

export default TopBar;
