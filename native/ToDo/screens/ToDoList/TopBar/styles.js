import { StyleSheet } from "react-native";

export default StyleSheet.create({
  topBar: {
    backgroundColor: "blue",
    fontWeight: "bold",
    fontSize: 30,
    color: "white",
    paddingTop: 15
  }
});
